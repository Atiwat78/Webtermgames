<?php
session_start();
include '../db.php'; // เชื่อมต่อฐานข้อมูล

// เช็คว่าเป็น Admin จริงไหม
if (!isset($_SESSION['role']) || $_SESSION['role'] !== 'admin') {
    header("Location: ../login.php");
    exit();
}

// เช็คว่ามี ID ส่งมาไหม
if (isset($_GET['id'])) {
    $id = $_GET['id'];

    // คำสั่งลบข้อมูล
    $sql = "DELETE FROM chapters WHERE id = $id";

    if ($conn->query($sql) === TRUE) {
        // ลบสำเร็จ ให้เด้งกลับไปหน้าจัดการ
        echo "<script>
                alert('🗑️ ลบตอนเรียบร้อยแล้ว'); 
                window.location='manage_chapters.php';
              </script>";
    } else {
        echo "เกิดข้อผิดพลาด: " . $conn->error;
    }
} else {
    // ถ้าไม่มี ID ส่งมา ให้เด้งกลับเลย
    header("Location: manage_chapters.php");
}
?>